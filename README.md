# Airport Ticket Booking System

A .NET console application for booking flight tickets. Passengers can search, book, and manage flights; managers can filter bookings, import flights via CSV with validation, and view dynamic validation rules for the flight model. Data is stored as JSON files on the file system.
